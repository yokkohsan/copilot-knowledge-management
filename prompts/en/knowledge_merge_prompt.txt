[How to use – read first]

1) Paste the entire [ACTIVE_KNOWLEDGES] block from ProjectX_MANIFEST.txt at the top of this prompt.
   - [ACTIVE_KNOWLEDGES] is expected to contain the currently active merged.md and any summary files that represent newer differences
   - Do not paste [HISTORY] unless explicitly instructed
2) If the pasted URLs are recognized as references or attachment-equivalent inputs, use their contents for this operation. If any URL cannot be referenced, do not guess; treat it as "not referenced".

---

You are merging knowledge for the same project.
Use only the files listed in [ACTIVE_KNOWLEDGES] and create or update the latest authoritative file, merged.md.
Do not use files listed in [HISTORY] as merge targets unless explicitly instructed.

No new discussion or additional analysis is needed.
Use only information that can be referenced.

[Important constraints]
- Do not add any new content
- Do not guess, infer, or generalize
- Do not rewrite or restructure content in a way that changes the meaning, and preserve the wording used in the source documents and artifacts
- Preserve artifacts contained in summary / merged files, such as documents, plans, designs, and program code, exactly as written
- If merged.md can be referenced:
  - Treat it as the current authoritative base
  - Treat summary files, where larger sequence numbers indicate newer knowledge, as differences added after merged.md
- If merged.md cannot be referenced:
  - Prioritize summary files by recency, where larger sequence numbers indicate newer knowledge
- Do not compress or delete information too aggressively on your own
- If a file cannot be referenced, do not guess; explicitly mark it as "not referenced"
- Accuracy is the highest priority

[Output policy]
- Output must be Markdown
- Output must have exactly two parts:
  1) Verification log (outside code blocks)
  2) merged.md content (inside one outer Markdown code block)
- Wrap the merged.md content in a single outer Markdown code block
- If artifacts include Markdown or program code, nest them properly inside the outer block
- Use a backtick length that does not conflict with inner code blocks

[Outer code block closing rules – important]
- Close the outer Markdown code block exactly once, at the very end of the merged.md content
- Do not output any characters, symbols, or blank lines after closing it
- If you write "(End)", it must appear as the final line inside the outer block
- Do not output extra backticks for safety or decoration after closing

[Artifact handling policy – important]
- The "Artifact snapshot" section in merge must include only primary artifacts that were actually created or edited
- Do not include the following as artifacts:
  - Knowledge backup text
  - Outputs generated by merge / backup / rescue
  - Structured knowledge-preservation documents such as "Knowledge Backup" or "Knowledge Merge"
- Treat the above as source material, not as artifacts, and do not include them in the artifact snapshot

[Output format]

The structure below is a guide for the output.
Keep the section titles, order, and structure.
The amount of content and nesting depth may vary depending on the source material.

# Verification Log (Merge)

## 1. Referenced files
- List referenced merged / summary files

## 2. Files not referenced (if any)
- File name or identifier:
  - URL:
  - Reason:

## 3. Merge decision notes
- Base file used:
- Summary files applied as differences:
- Conflict resolution rules:
- Invalidated or excluded assumptions / conclusions, and reasons:
- Whether duplicate artifacts were merged:
- Included, adopted, or excluded artifacts, and reasons:

(From this point onward, write the merged.md content. Wrap everything below in one outer Markdown code block.)

# Knowledge Merge

## 0. Position of this merged file
- This merged.md is the authoritative file reused for restore

## 1. Purpose of the conversation
- Describe the purpose of the conversations or discussions being merged

## 2. Assumptions and constraints
- List valid assumptions and constraints

## 3. Decisions and conclusions reached
- List currently valid decisions and conclusions

## 4. Open issues and pending items
- List unresolved issues, if any
- If none, explicitly write "none"

## 5. Referenced materials
- List materials that remain valid after the merge, as far as they can be identified

(Example)
- Material name:
  - Type:
  - Storage location:
  - Reference URL:
  - Role in the conversation:

## 6. Artifact snapshot (no summarizing or editing)
- Include only primary artifacts that were actually created or edited during the conversations or discussions
- Primary artifacts include:
  - Program code
  - Design documents, specifications, and plans
  - Reusable written drafts
- Do not include the following as artifacts:
  - Knowledge backup text
  - Knowledge merge files such as merged.md, or outputs generated by rescue / backup
  - Structured documents that simply copy those outputs
- If multiple artifacts exist, separate them clearly using headings
- If multiple substantially identical versions of an artifact exist, place the latest version first as the authoritative version
- Do not delete older artifacts; distinguish them as older versions or for difference checking
- If there are differences, include them in a way that makes the differences clear
- If no clear primary artifacts exist, write "none"
- Artifact headings must use "Artifact 1", "Artifact 2", etc. (no numbering like 6-1 or 6.1)

(Example)
### Artifact 1 (primary artifact example)
The following is an example of a primary artifact actually created during the conversation or discussion.
Write the original content here without summarizing or editing it.

(write the original document, specification, draft, or program code here)

Note: Knowledge backup text and outputs generated by merge / rescue / backup must not be included as artifacts.

(End)
